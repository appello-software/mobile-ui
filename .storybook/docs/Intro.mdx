# Intro
This package contains:
- Reusable components that you could use across our apps. You can find the list of them and their documentation on the left
- Functions for setting up the app Theme and the components' config with correct types. [You can find more information here](/docs/settingupconfig--docs)
- Some additional utils such us set of reusable styles and `chroma` function to work with colors
